# Deferred skills / rules

Candidates considered from insights + Phase 4 but **not** installed as high-frequency workflow artifacts.

| Candidate | Why deferred |
|-----------|----------------|
| `todo-init-for-complex` | Useful on long turns, but not a default always-on habit; TodoWrite already used opportunistically |
| `cross-agent-tool-alias` | Meta mapping of Cursor↔Grok tool names; better as a short note in portable skills than a standalone skill |
| `grok-slash-plan` / `/plan` wrapper | Overlaps Grok built-ins (`/plan`, `/goal`); no new procedure |
| `eagle-satellite-handoff` | Already present under `~/.grok/skills` / `~/.agents/skills`; no need to duplicate |
| Project skills for `home-mixer`, `params.rs`, filename-like bundles | Noise `repo_hint`s from Kilo/context files — not real project bundles |
| Niche project one-offs (extension rename checklists, zod migrations, etc.) | Keep under existing `docs/artifacts/<bundle>/` until that project’s weekly workflow needs a refresh |

Revisit after the next harvest if a pattern appears in top tool sequences **and** across multiple repos.
